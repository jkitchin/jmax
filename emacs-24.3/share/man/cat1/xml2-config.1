GNOME-XML(1)                         1.1.0                        GNOME-XML(1)



NNAAMMEE
       xml-config  -  script to get information about the installed version of
       GNOME-XML

SSYYNNOOPPSSIISS
       xxmmll--ccoonnffiigg [--prefix_[_=_D_I_R_]] [--libs] [--cflags] [--version] [--help]

DDEESSCCRRIIPPTTIIOONN
       _x_m_l_-_c_o_n_f_i_g is a tool that is used to determine the compile  and  linker
       flags  that should be used to compile and link programs that use _G_N_O_M_E_-
       _X_M_L.

OOPPTTIIOONNSS
       _x_m_l_-_c_o_n_f_i_g accepts the following options:

       ----vveerrssiioonn
               Print the currently installed version of _G_N_O_M_E_-_X_M_L on the stan-
               dard output.

       ----lliibbss  Print  the  linker flags that are necessary to link a _G_N_O_M_E_-_X_M_L
               program.

       ----ccffllaaggss
               Print the compiler flags that are necessary to compile a _G_N_O_M_E_-
               _X_M_L program.

       ----pprreeffiixx==PPRREEFFIIXX
               If  specified,  use  PREFIX  instead of the installation prefix
               that _G_N_O_M_E_-_X_M_L was built with when computing the output for the
               --cflags  and  --libs  options.  This  option must be specified
               before any --libs or --cflags options.

AAUUTTHHOORR
       This  manual   page   was   written   by   Fredrik   Hallenberg   <hal-
       lon@lysator.liu.se>,  for  the Debian GNU/linux system (but may be used
       by others).



Version                           3 July 1999                     GNOME-XML(1)
